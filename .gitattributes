# 统一用 LF 行尾。
# Cocos 的 .meta 是 JSON，行尾不统一会导致大量无意义的 diff，
# 而且 Windows/Mac 混开时会反复互相覆盖。
* text=auto eol=lf

*.ts   text eol=lf
*.js   text eol=lf
*.json text eol=lf
*.meta text eol=lf
*.md   text eol=lf

# 二进制资源，不做行尾转换
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.mp3  binary
*.ogg  binary
*.wav  binary
*.glb  binary
*.gltf binary
*.fbx  binary
*.bin  binary
